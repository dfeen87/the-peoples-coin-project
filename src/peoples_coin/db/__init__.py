from .database import db

# You can also define your declarative base here if you need it for models
# that don't inherit from db.Model, but it's often not necessary.
# from sqlalchemy.orm import declarative_base
# Base = declarative_base()
